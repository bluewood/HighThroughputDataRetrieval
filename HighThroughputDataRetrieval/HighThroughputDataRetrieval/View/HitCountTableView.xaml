<Window x:Class="HighThroughputDataRetrieval.View.HitCountTableView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="HitCountTableView" Height="300" Width="300">
    
    <ListBox Width="200" Margin="10"
         ItemsSource="{Binding CountNumbers}"/>


    <!--<Window.Background>
        <SolidColorBrush Color="LightBlue" Opacity="0.25"></SolidColorBrush>
    
    </Window.Background>-->
    <!--<DockPanel>
        <DataGrid DockPanel.Dock="Top" Height="300" AutoGenerateColumns="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Testing" Binding="{Binding countNumbers}" Width="0.1*"/>   
            </DataGrid.Columns>
        </DataGrid>
    </DockPanel>-->

    <!--<Grid>
    <ItemsControl ItemsSource="{Binding countNumbers}"></ItemsControl>
    </Grid>-->
</Window>
